/*module.exports = {
	name: 'beans',
    description: 'Make a certain someone feel complete and utter pain through beans...',
    cooldown: 50000,
    usage: '<user>',
	execute(message) {
        message.guild.members.fetch('283068026960609283').then(a => {
            a.user.send(`${message.author.username} sent you... https://i.imgur.com/SjuHlf6.png`);
            for (let i = 0; i < 5; i++) {
                a.user.send('https://i.imgur.com/Dd9j4pe.png');
            }
        }, 
        );
        message.channel.send('Sending a special someone a special package...');
        
	},
};*/